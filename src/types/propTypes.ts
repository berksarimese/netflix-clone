export type modalProp = {
    mediaType: string,
    showId: number,
    showImage: string,
    showTitle: string,
    showDescription: string,
    showDate: string,
    genre: number[],
    active: boolean,
}